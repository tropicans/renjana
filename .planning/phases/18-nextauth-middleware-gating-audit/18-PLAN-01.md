---
phase: 18
plan: "18-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth.config.ts
  - src/lib/auth.ts
  - src/proxy.ts
  - src/middleware.ts
  - tests/middleware-gating.test.ts
autonomous: true
requirements: ["AUDIT-10"]
must_haves:
  truths:
    - "NextAuth configuration is split into auth.config.ts and auth.ts"
    - "src/middleware.ts is created and exports the Edge-safe auth middleware"
    - "src/proxy.ts imports auth from '@/lib/auth.config'"
    - "Pages in admin/instructor/manager/finance/dashboard are guarded by middleware and redirect correctly"
  artifacts:
    - path: "src/lib/auth.config.ts"
      provides: "Edge-safe NextAuth configuration"
    - path: "src/middleware.ts"
      provides: "Global Next.js Edge middleware"
    - path: "tests/middleware-gating.test.ts"
      provides: "Vitest test suite for middleware route protection"
  key_links:
    - from: "src/middleware.ts"
      to: "src/proxy.ts"
      via: "import proxy"
---

<objective>
Restore server-side global middleware route gating by splitting NextAuth configuration into Edge-compatible and Node.js-specific modules, activating src/middleware.ts, and establishing test coverage.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-nextauth-middleware-gating-audit/18-CONTEXT.md
@src/proxy.ts
@src/lib/auth.ts
@src/lib/auth-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split NextAuth configuration and create Edge-compatible config</name>
  <files>src/lib/auth.config.ts, src/lib/auth.ts</files>
  <action>
    - Create `src/lib/auth.config.ts` containing callbacks (`jwt`, `session`), `pages` routing config, JWT `session` strategy, empty `providers` array, and the dynamic `secret` key. Export `{ auth }`.
    - Refactor `src/lib/auth.ts` to extend `authConfig` configuration and append the database/bcrypt `Credentials` provider.
  </action>
  <verify>
    <automated>npx vitest run tests/role-routing.test.ts</automated>
  </verify>
  <done>
    - NextAuth configuration is split successfully into `auth.config.ts` and `auth.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor proxy and activate middleware</name>
  <files>src/proxy.ts, src/middleware.ts</files>
  <action>
    - Refactor the import at line 1 of `src/proxy.ts` to import `auth` from `@/lib/auth.config` instead of `@/lib/auth`.
    - Create `src/middleware.ts` exporting `proxy` as default from `./proxy` and exporting the `config` matcher.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    - `middleware.ts` is created and imports from `proxy.ts`.
    - `proxy.ts` imports from Edge-safe configuration module.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement middleware gating tests</name>
  <files>tests/middleware-gating.test.ts</files>
  <action>
    - Create `tests/middleware-gating.test.ts` to cover the global middleware protection:
      - Public routes (e.g. `/`, `/login`) are allowed.
      - Unauthenticated requests to protected portals are redirected to `/login`.
      - Authenticated users with incorrect roles (e.g., `LEARNER` requesting `/admin`) are redirected to their correct portal home (e.g., `/dashboard`).
  </action>
  <verify>
    <automated>npx vitest run tests/middleware-gating.test.ts</automated>
  </verify>
  <done>
    - Middleware gating tests pass successfully.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Application | Middleware intercepts page requests at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege | src/middleware.ts | mitigate | Enforce role-based checks and automatically redirect unauthorized users to their home portals. |
| T-18-02 | Spoofing | src/middleware.ts | mitigate | Ensure unauthenticated page requests are redirected to `/login`. |
</threat_model>

<verification>
npx vitest run tests/middleware-gating.test.ts
npm run lint
npm run build
</verification>

<success_criteria>
- NextAuth configuration is split to support Vercel Edge Runtime.
- Global server-side middleware `middleware.ts` is active and functional.
- Unauthenticated requests are blocked.
- Unauthorized pages redirects route users back to their respective dashboards.
</success_criteria>

<output>
Create `.planning/phases/18-nextauth-middleware-gating-audit/18-18-01-SUMMARY.md` when done
</output>
