---
phase: 04-landing-page-intro-basic-learner-info
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/context/user-context.tsx
  - src/app/dashboard/page.tsx
  - src/components/ui/learning-methods-section.tsx
autonomous: true
requirements:
  - INTRO-01
  - INTRO-02
  - INTRO-03
  - LEARN-01
  - LEARN-06
user_setup: []

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Landing page displays LMS & LXP and 4 learning methods with hover transitions and visual borders."
    - "Learner dashboard displays a profile card with user name, email, role badge, and initials avatar."
    - "Learner dashboard displays a dedicated section for evidence review containing uploaded assignments with comments and ratings."
  artifacts:
    - path: "src/lib/context/user-context.tsx"
      provides: "session image mapping to client-side user context"
    - path: "src/app/dashboard/page.tsx"
      provides: "User identity card and Ulasan & Feedback Tugas section UI"
    - path: "src/components/ui/learning-methods-section.tsx"
      provides: "premium visual effects for learning methods cards"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "/api/evidence"
      via: "fetchEvidences call using TanStack Query"
      pattern: "useQuery.*queryKey:.*evidences"
---

<objective>
Implement the Landing Page introduction enhancements, and build the Learner Dashboard User Identity card and the Graded Evidence feedback review section.

Purpose: Provide learners with a visual identity section and direct visibility of graded evidence files, feedback ratings, and comments.
Output: Dynamic User profile details display, active/pending evidence lists, and interactive UI styling.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-landing-page-intro-basic-learner-info/04-CONTEXT.md
@.planning/phases/04-landing-page-intro-basic-learner-info/04-UI-SPEC.md
@src/lib/context/user-context.tsx
@src/app/dashboard/page.tsx
@src/components/ui/learning-methods-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose avatar URL in client-side User Context</name>
  <files>src/lib/context/user-context.tsx</files>
  <read_first>src/lib/context/user-context.tsx</read_first>
  <action>
    Add `avatarUrl?: string | null` to the client-side `User` interface.
    Map `avatarUrl: session.user.image ?? null` in the `session` to client `user` mapping within the `UserProvider` component.
  </action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - `src/lib/context/user-context.tsx` exports User interface containing `avatarUrl`
    - `UserProvider` sets `avatarUrl` from `session.user.image`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build User Identity Profile Card at Dashboard Header</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>src/app/dashboard/page.tsx, src/lib/context/user-context.tsx</read_first>
  <action>
    Modify `src/app/dashboard/page.tsx` to add a premium card layout at the top, integrating the user identity info.
    It must display:
    - User avatar: a circular circle containing the user's name initials (e.g. "JD" for John Doe) or an image if `user.avatarUrl` exists.
    - User's full name: `user.name`
    - User's email: `user.email`
    - Role badge: A badge showing the user's role (e.g. `LEARNER`) styled with brand colors.
    The card must be fully responsive, blending seamlessly with the welcome section.
  </action>
  <verify>npm run build passes without errors</verify>
  <acceptance_criteria>
    - `src/app/dashboard/page.tsx` renders a profile section with avatar, name, email, and role badge
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build Graded Evidence Feedback Section</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>src/app/dashboard/page.tsx, src/lib/api.ts</read_first>
  <action>
    In `src/app/dashboard/page.tsx`, use React Query to fetch learner evidences:
    ```typescript
    const { data: evidenceData, isLoading: evidencesLoading } = useQuery({
        queryKey: ["my-evidences"],
        queryFn: fetchEvidences,
        ...learnerQueryOptions,
    });
    ```
    (Import `fetchEvidences` from `@/features/client/api/learner` or `@/lib/api`).
    Add a section titled "Ulasan & Feedback Tugas" on the dashboard:
    - Display all submitted evidence records.
    - Graded items show: Title, upload date, a row of star icons representing the rating (1-5), and the instructor's comment.
    - Pending items show: Title, upload date, and a "Menunggu Penilaian" status badge.
    - Handle empty state: display a message if the list is empty: "Belum ada ulasan tugas. Kirim bukti tugas atau evidence pembelajaran di kelas untuk mendapatkan penilaian dan komentar dari instruktur."
  </action>
  <verify>npm run build succeeds</verify>
  <acceptance_criteria>
    - `src/app/dashboard/page.tsx` queries `["my-evidences"]` using `fetchEvidences`
    - Renders the "Ulasan & Feedback Tugas" section with correct status badges, star rating counts, and empty state message
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Enhance Learning Methods Section visual styling</name>
  <files>src/components/ui/learning-methods-section.tsx</files>
  <read_first>src/components/ui/learning-methods-section.tsx</read_first>
  <action>
    Add premium style enhancements to the 4 method cards (Mandiri, Online, Offline, Hybrid):
    - Smooth hover scaling transition (`hover:scale-[1.02] duration-300`).
    - Subtle borders that highlight on hover (`hover:border-primary/50`).
    - Subtle brand shadow/glow on hover.
  </action>
  <verify>npm run build succeeds</verify>
  <acceptance_criteria>
    - `src/components/ui/learning-methods-section.tsx` uses Tailwind hover scale and border colors transitions on method cards
  </acceptance_criteria>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm run lint`
- [ ] Run `npm run build`
- [ ] Run all Vitest suites: `npm run test`
</verification>

<success_criteria>
- User Identity card is successfully integrated into the dashboard header.
- Learner's graded evidence reviews (ratings and comments) are displayed under the "Ulasan & Feedback Tugas" section.
- Empty states are correctly handled.
- Learning methods cards on the landing page have premium visual hover effects.
- Test suite and lint checks pass with 100% success rate.
</success_criteria>

## Artifacts this phase produces

This phase does not create new files. It modifies existing files to produce the following features and symbols:
- `User.avatarUrl` property on client-side `User` interface in `src/lib/context/user-context.tsx`.
- Profile Header component inside `src/app/dashboard/page.tsx` for displaying user details.
- Graded Evidence list section ("Ulasan & Feedback Tugas") inside `src/app/dashboard/page.tsx`.

<output>
After completion, create `.planning/phases/04-landing-page-intro-basic-learner-info/04-01-SUMMARY.md`
</output>
